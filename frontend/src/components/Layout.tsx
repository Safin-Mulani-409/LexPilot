import { BriefcaseBusiness, FilePlus2, LayoutDashboard, Scale } from "lucide-react";
import { Link, useLocation } from "react-router-dom";
import type { ReactNode } from "react";

export function Layout({ children }: { children: ReactNode }) { const location = useLocation(); const isUpload = location.pathname === "/upload"; return <div className="min-h-screen bg-[radial-gradient(circle_at_20%_-10%,#27272a,transparent_30%)]"><aside className="fixed inset-y-0 hidden w-64 border-r border-zinc-800 bg-zinc-950/70 p-6 lg:block"><Link to="/" className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-amber text-zinc-950"><Scale size={21}/></span><span><strong className="block text-lg tracking-tight">LexPilot</strong><small className="text-zinc-500">AI Junior Advocate</small></span></Link><nav className="mt-12 space-y-2"><Nav to="/" active={!isUpload} icon={<LayoutDashboard size={18}/>} label="Workspace"/><Nav to="/upload" active={isUpload} icon={<FilePlus2 size={18}/>} label="New case"/></nav><div className="absolute bottom-7 left-6 right-6 rounded-xl border border-zinc-800 bg-zinc-900/50 p-4"><p className="text-xs leading-5 text-zinc-500">AI-generated work product must always be reviewed by a qualified advocate.</p></div></aside><main className="lg:pl-64"><header className="flex h-20 items-center justify-between border-b border-zinc-800/80 px-5 sm:px-8"><div className="flex items-center gap-3 lg:hidden"><Scale className="text-amber"/><span className="font-semibold">LexPilot</span></div><p className="hidden text-sm text-zinc-500 sm:block">Legal intelligence workspace</p><Link to="/upload" className="button-primary"><FilePlus2 size={16}/> Analyse case</Link></header><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8">{children}</div></main></div> }
function Nav({ to, active, icon, label }: { to:string; active:boolean; icon:ReactNode; label:string }) { return <Link to={to} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-sm transition ${active ? "bg-zinc-800 text-white" : "text-zinc-500 hover:bg-zinc-900 hover:text-zinc-200"}`}>{icon}{label}</Link> }
